# Agent Rules

## 项目背景
这是一个使用 NestJS、面向 AI 应用工程师转型的实践项目。

## 代码规范
- 代码缩进统一使用 4 个空格。
- 函数命名统一使用 camelCase。

## API 命名规范
- API 统一使用 `POST` 请求方式。
- 接口设计需符合 REST 风格规范。

## 文档维护规范
- 每次修改代码后，必须自动更新 `README` 中的 API 文档，确保文档与实现保持一致。

## PR 规范
- PR 标题命名方式统一为：`chore/feat/fix: XXX`。
